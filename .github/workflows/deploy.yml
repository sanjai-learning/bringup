name: Deploy Platform

on:
  push:
    branches: [main]

env:
  REGISTRY: ghcr.io
  IMAGE_PREFIX: ghcr.io/${{ github.repository }}

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push platform-mcp
        uses: docker/build-push-action@v6
        with:
          context: .
          file: Dockerfile.mcp
          push: true
          tags: ${{ env.IMAGE_PREFIX }}/platform-mcp:latest

      - name: Build and push web-ui
        uses: docker/build-push-action@v6
        with:
          context: .
          file: Dockerfile.web
          push: true
          tags: ${{ env.IMAGE_PREFIX }}/web-ui:latest

  deploy:
    runs-on: [self-hosted, linux, x64, vm526300408]
    needs: build-and-push

    steps:
      - name: Pull latest code
        run: |
          cd /opt/bringup
          git fetch origin main
          git reset --hard origin/main

      - name: Log in to GitHub Container Registry
        run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u "${{ github.actor }}" --password-stdin

      - name: Deploy with Docker Compose
        run: |
          cd /opt/bringup

          # Ensure .env exists with secrets
          if [ ! -f .env ]; then
            echo "ERROR: .env file not found at /opt/bringup/.env"
            echo "Create it from .env.example with real values."
            exit 1
          fi

          # Build and start services
          docker compose build
          docker compose up -d --remove-orphans

      - name: Verify services are running
        run: |
          cd /opt/bringup
          sleep 10
          docker compose ps
          echo "--- Health check ---"
          curl -sf http://localhost:3000/ > /dev/null && echo "web-ui: OK" || echo "web-ui: FAILED"
          curl -sf http://localhost:2016/ -o /dev/null -w "%{http_code}" && echo " oauth2-proxy: OK" || echo "oauth2-proxy: FAILED"
